import styled from 'styled-components';

export const ResponsiveWrapper = styled.div`
  width: 100%;
  overflow-x: auto;
  height: ${props => props.limit * 87}px;
`;

export const PaginatorWrapper = styled.div`
  width: 100%;
  padding: 10px 5px;
`;

export const CommonTableWrapper = styled.div``;
